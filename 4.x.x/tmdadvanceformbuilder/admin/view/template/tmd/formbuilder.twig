{{ header }}{{ column_left }}
 {% set form_option_row = 0 %}   
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="float-end"><button type="submit" form="form-formbuilder" data-bs-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fas fa-save"></i></button>
        <a href="{{ back }}" data-bs-toggle="tooltip" title="{{ button_back }}" class="btn btn-light"><i class="fas fa-reply"></i></a>
      </div>
      <h1>{{ heading_title }}</h1>
      <ol class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ol>
    </div>
  </div>
  <div class="container-fluid">
    <div class="card">
      <div class="card-header">
         <i class="fas fa-pencil-alt"></i>
          {{ text_form }}
      </div>
      <div class="card-body" id="form-form">
        <form id="form-formbuilder" action="{{ save }}" method="post" data-oc-toggle="ajax" >
          
          <ul class="nav nav-tabs">
            <li class="nav-item"><a href="#tab-general" data-bs-toggle="tab" class="nav-link active"><i class="fa fa-language" aria-hidden="true"></i> {{ tab_language }}</a></li>
            <li class="nav-item"><a href="#tab-setting" data-bs-toggle="tab" class="nav-link"><i class="fa fa-cog" aria-hidden="true"></i>{{ tab_setting }}</a></li>
            <li class="nav-item">
          <a href="#tab-formfield" data-bs-toggle="tab" class="nav-link">
              <i class="fa fa-square" aria-hidden="true"></i>
             {{ tab_formfield }}
          </a>
      </li>
            <li class="nav-item"><a href="#tab-link" data-bs-toggle="tab" class="nav-link"><i class="fa fa-link" aria-hidden="true"></i>{{ tab_link }}</a></li>
            
            <li class="nav-item"><a href="#tab-notify" data-bs-toggle="tab" class="nav-link"><i class="fa fa-exclamation-circle" aria-hidden="true"></i>{{ tab_notify }}</a></li>
            <li class="nav-item"><a href="#tab-success" data-bs-toggle="tab" class="nav-link"><i class="fa fa-envelope" aria-hidden="true"></i>{{ tab_success }}</a></li>
            <li class="nav-item"><a href="#tab-seo" data-bs-toggle="tab" class="nav-link"><i class="fa fa-cogs" aria-hidden="true"></i> {{ tab_seo }}</a></li>
            <li class="nav-item"><a href="#tab-custome" data-bs-toggle="tab" class="nav-link"><i class="fa fa-pencil-square" aria-hidden="true"></i>{{ tab_custome }}</a></li>
          </ul>
          <div class="tab-content">
            <div id="tab-general" class="tab-pane active">
              <ul class="nav nav-tabs" id="language">
                {% for language in languages %}
                  <li class="nav-item"><a href="#language-{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/> {{ language.name }}</a></li>
                {% endfor %}
              </ul>
              <div class="tab-content" class="tab-pane">
                {% for language in languages %}
                  <div id="language-{{ language.language_id }}" class="tab-pane{% if loop.first %} active{% endif %}">
                    
                    <div class="row mb-3 required">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_title }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][title]" value="{{ form_description[language.language_id] ? form_description[language.language_id].title }}" placeholder="{{ entry_title }}" id="input-title{{ language.language_id }}" class="form-control" />
                      <div id="error-title-{{ language.language_id }}" class="invalid-feedback"></div>
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_metatitle }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][meta_title]" value="{{ form_description[language.language_id] ? form_description[language.language_id].meta_title }}" placeholder="{{ entry_metatitle }}" id="input-description{{ language.language_id }}" class="form-control" />
                      </div>
                    </div>

                <div class="row mb-3 ">
                  <label for="input-name" class="col-sm-2 col-form-label">{{ entry_metakeyword }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="form_description[{{ language.language_id }}][meta_keyword]" value="{{ form_description[language.language_id] ? form_description[language.language_id].meta_keyword }}" placeholder="{{ entry_metakeyword }}" id="input-meta_keyword{{ language.language_id }}" class="form-control" />
                  </div>
                </div>

                    <div class="row mb-3 ">
                      <label for="input-name" class="col-sm-2 col-form-label">{{ entry_metadesc }}</label>
                      <div class="col-sm-10">
                        <textarea name="form_description[{{ language.language_id }}][meta_description]" placeholder="{{ entry_metadesc }}" id="input-meta_description{{ language.language_id }}"  class="form-control">{{ form_description[language.language_id] ? form_description[language.language_id].meta_description }}</textarea>
                      </div>
                    </div>
                    
                    <div class="row mb-3 ">
                      <label for="input-name" class="col-sm-2 col-form-label">{{ entry_topdesc }}</label>
                      <div class="col-sm-10">
                        <textarea name="form_description[{{ language.language_id }}][top_description]" placeholder="{{ entry_topdesc }}" value="" id="input-top_description{{ language.language_id }}"  data-oc-toggle="ckeditor" data-lang="{{ ckeditor }}"  class="form-control">{{ form_description[language.language_id] ? form_description[language.language_id].top_description }}
                        </textarea>
                        <div class="form-text">{{ help_topdesc }}</div>
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_description }}</label>
                      <div class="col-sm-10">
                        <textarea name="form_description[{{ language.language_id }}][bottom_description]" placeholder="{{ entry_description }}" value="" id="input-bottom_description{{ language.language_id }}" data-oc-toggle="ckeditor" data-lang="{{ ckeditor }}"  class="form-control">{{ form_description[language.language_id] ? form_description[language.language_id].bottom_description }}</textarea>
                        <div class="form-text">{{ help_botomdesc }}</div>
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_buttonname }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][button_name]" value="{{ form_description[language.language_id] ? form_description[language.language_id].button_name }}" placeholder="{{ entry_buttonname }}" id="input-title{{ language.language_id }}" class="form-control" />  
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_resetbuttonname }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][resetbuttonname]" value="{{ form_description[language.language_id] ? form_description[language.language_id].resetbuttonname }}" placeholder="{{ entry_resetbuttonname }}" id="input-title{{ language.language_id }}" class="form-control" />  
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_popuplinkname }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][popuplinkname]" value="{{ form_description[language.language_id] ? form_description[language.language_id].popuplinkname }}" placeholder="{{ entry_popuplinkname }}" id="input-title{{ language.language_id }}" class="form-control"/>  
                      </div>
                    </div>

                    <div class="row mb-3 ">
                      <label for="input-name-{{ language.language_id }}" class="col-sm-2 col-form-label">{{ entry_uplaodfilebtn }}</label>
                      <div class="col-sm-10">
                        <input type="text" name="form_description[{{ language.language_id }}][uplaodfilebtn]" value="{{ form_description[language.language_id] ? form_description[language.language_id].uplaodfilebtn }}" placeholder="{{ entry_uplaodfilebtn }}" id="input-title{{ language.language_id }}" class="form-control"/>  
                      </div>
                    </div>
                    
                  </div>
                {% endfor %}
              </div>
            </div>
           
            <div id="tab-setting" class="tab-pane">
              
              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_header }}</label>
                <div class="col-sm-4">
                <label class="radio-inline">
                  {% if headerlink==1 %}
                  <input type="checkbox" name="headerlink" value="1" checked="checked"/>
                  {% else %}
                  <input type="checkbox" name="headerlink" value="1"/>
                  {% endif %}
                 </label> 
                  <div class="form-text">{{ help_header }}</div>
                </div>
             
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_footer }}</label>
                <div class="col-sm-4">
                <label class="radio-inline">
                  {% if footerlink==1 %}
                  <input type="checkbox" name="footerlink" value="1" checked="checked"/>
                  {% else %}
                  <input type="checkbox" name="footerlink" value="1"/>
                  {% endif %} 
                 </label>
                  <div class="form-text">{{ help_footer }}</div> 
                </div>
              </div>

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_productsize }}</label>
                <div class="col-sm-10">
                  <div class="row">
                    <div class="col-sm-6">
                    <input type="text" name="productwidth" value="{{ productwidth }}" placeholder="{{ entry_width }}" id="input-image-location" class="form-control" />
                    </div>
                    <div class="col-sm-6">
                    <input type="text" name="productheight" value="{{ productheight }}" placeholder="{{ entry_height }}" class="form-control" />
                    </div>
                  </div>
                   <div class="form-text">{{ help_productsize }}</div>
                </div>
              </div>

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_resetbutton }}</label>
                <div class="col-sm-10">
                  <select name="resetbutton" id="input-resetbutton" class="form-select">
                    {% if resetbutton %}
                    <option value="1" selected="selected">{{ text_yes }}</option>
                    <option value="0">{{ text_no }}</option>
                    {% else %}
                    <option value="1">{{ text_yes }}</option>
                    <option value="0" selected="selected">{{ text_no }}</option>
                    {% endif %}
                  </select>
                   <div class="form-text">{{ help_resetbutton }}</div>
                </div>
              </div>

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_captcha }}</label>
                <div class="col-sm-10">
                  <select name="captcha" id="input-captcha" class="form-select">
                    {% if captcha %}
                    <option value="1" selected="selected">{{ text_yes }}</option>
                    <option value="0">{{ text_no }}</option>
                    {% else %}
                    <option value="1">{{ text_yes }}</option>
                    <option value="0" selected="selected">{{ text_no }}</option>
                    {% endif %}
                  </select>
                   <div class="form-text">{{ help_captcha }}</div>
                </div>
              </div>


              {% if(VERSION >='4.0.2.0') %}
                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-form-product-id">{{ entry_product }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="form_product" value="{{ form_product }}" placeholder="{{ entry_product }}" id="input-form-product-id" data-oc-target="autocomplete-form_product" class="form-control" autocomplete="off"/>
                    <input type="hidden" name="product_id" value="{{ product_id }}"/>
                     <ul id="autocomplete-form_product" class="dropdown-menu"></ul>
                  </div>  
                </div>

              {% else %}
                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-form-product-id">{{ entry_product }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="form_product" value="{{ form_product }}" placeholder="{{ entry_product }}" id="input-form-product-id" list="list-form_product" class="form-control"/>
                    <input type="hidden" name="product_id" value="{{ product_id }}"/>
                    <datalist id="list-form_product"></datalist>
                  </div>  
                </div>
              {% endif %}

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-price">{{ entry_base_price }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="price" value="{{ price }}" id="input-price" placeholder="{{ entry_price }}" class="form-control"/>
                  </div>
                </div>

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_status }}</label>
                <div class="col-sm-10">
                  <select name="status" id="input-status" class="form-select">
                    {% if status %}
                    <option value="1" selected="selected">{{ text_enable }}</option>
                    <option value="0">{{ text_disable }}</option>
                    {% else %}
                    <option value="1">{{ text_enable }}</option>
                    <option value="0" selected="selected">{{ text_disable }}</option>
                    {% endif %}
                  </select>
                </div>
              </div>   

              <div class="row mb-3">
                <label class="col-sm-2 col-form-label" for="input-bgcolor">{{ entry_bgcolor }}</label>
                <div class="col-sm-4">
                <input type="text" name="bgcolor" value="{{ bgcolor }}" placeholder="{{ entry_bgcolor }}" class="form-control color" />
                </div>
              
                <label class="col-sm-2 col-form-label" for="input-txtcolor">{{ entry_txtcolor }}</label>
                <div class="col-sm-4">
                <input type="text" name="txtcolor" value="{{ txtcolor }}" placeholder="{{ entry_txtcolor }}" class="form-control color" />
                </div>
              </div>

              <div class="row mb-3">
                <label class="col-sm-2 col-form-label" for="input-bgcolor">{{ entry_submit_bgcolor }}</label>
                <div class="col-sm-4">
                <input type="text" name="submit_bgcolor" value="{{ submit_bgcolor }}" placeholder="{{ entry_submit_bgcolor }}" class="form-control color" />
                </div>
              
                <label class="col-sm-2 col-form-label" for="input-txtcolor">{{ entry_submit_txtcolor }}</label>
                <div class="col-sm-4">
                <input type="text" name="submit_txtcolor" value="{{ submit_txtcolor }}" placeholder="{{ entry_submit_txtcolor }}" class="form-control color" />
                </div>
              </div>

              <div class="row mb-3">
                <label class="col-sm-2 col-form-label" for="input-bgcolor">{{ entry_reset_bgcolor }}</label>
                <div class="col-sm-4">
                <input type="text" name="reset_bgcolor" value="{{ reset_bgcolor }}" placeholder="{{ entry_reset_bgcolor }}" class="form-control color" />
                </div>
              
                <label class="col-sm-2 col-form-label" for="input-txtcolor">{{ entry_reset_txtcolor }}</label>
                <div class="col-sm-4">
                  <input type="text" name="reset_txtcolor" value="{{ reset_txtcolor }}" placeholder="{{ entry_reset_txtcolor }}" class="form-control color" />
                </div>
              </div>
            </div>
<!-- pending -->
 <div class="tab-pane" id="tab-formfield">
  
    <div class="row">
      <div class="col-sm-2">
        <ul class="nav nav-pills nav-stacked flex-column" id="formfield">
          {% set form_row = 0 %}
             {% for option_fields in optionfieldss %}
             <li class="nav-item"><a href="#tab-formfield{{ form_row }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><i class="fa fa-minus-circle delete" rel="{{ option_fields.field_id }}" onclick="$('#formfield a:first').tab('show'); $('#formfield a[href=\'#tab-formfield{{ form_row }}\']').parent().remove(); $('#tab-formfield{{ form_row }}').remove();"> </i>
            {% if option_fields.form_field_description[config_language_id].field_name %}
            {{ option_fields.form_field_description[config_language_id].field_name }}
            {% else %}
            {{ tab_option.form_row }} 
            {% endif %}
            </a></li>
            {% set form_row = form_row + 1 %}
          {% endfor %}
            <li id="formfield-add">
      <a onclick="addFormfield();" class="btn btn-primary"><i class="fa fa-plus-circle"></i> {{ button_address_add }}</a>
      </li>
        </ul>
      </div>
             
    <div class="col-sm-10">
      <div class="tab-content one">
      {% set form_row = 0 %}
        {% if optionfieldss %}
          {% for option_fields in optionfieldss %}
            
        
      <div class="tab-pane{% if loop.first %} active{% endif %}" id="tab-formfield{{ form_row }}">
        <ul class="nav nav-tabs fieldslanguage" id="forms_fields{{ form_row }}">
          {% for language in languages %}
            <li class="nav-item"><a href="#forms_fields{{ form_row }}-{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}" /> {{ language.name }}</a></li>
          {% endfor %}
        </ul>
          
        <div class="tab-content">
         <input type="hidden" name="option_fields[{{ form_row }}][field_id]" value="{{ option_fields.field_id }}">

           {% for language in languages %}     
            <div id="forms_fields{{ form_row }}-{{ language.language_id }}" class="tab-pane{% if loop.first %} active{% endif %}">      
            <div class="row mb-3">    
              <label class="col-sm-2 col-form-label" for="input-field_name{{ form_row }}">{{ entry_fieldname }}</label>
              <div class="col-sm-10">
              <input type="text" name="option_fields[{{ form_row }}][form_fields][{{ language.language_id }}][field_name]" value="{{ option_fields.form_field_description[language.language_id] ? option_fields.form_field_description[language.language_id].field_name }}" placeholder="{{ entry_fieldname }}" id="input-field_name{{ form_row }}" class="form-control" />
              <div class="form-text">{{ help_fieldname }}</div> 
              </div> 
            </div>
            <div class="row mb-3">
              <label class="col-sm-2 col-form-label" for="input-help_text{{ form_row }}">{{ entry_helptext }}</label>
              <div class="col-sm-10">
              <input type="text" name="option_fields[{{ form_row }}][form_fields][{{ language.language_id }}][help_text]" value="{{ option_fields.form_field_description[language.language_id] ? option_fields.form_field_description[language.language_id].help_text }}" placeholder="{{ entry_helptext }}" id="input-help_text{{ form_row }}" class="form-control" />
              <div class="form-text">{{ help_helptext }}</div>
              </div>  
            </div>
            <div class="row mb-3">
              <label class="col-sm-2 col-form-label" for="input-placeholder{{ form_row }}">{{ entry_placeholder }}</label>
              <div class="col-sm-10">
              <input type="text" name="option_fields[{{ form_row }}][form_fields][{{ language.language_id }}][placeholder]" value="{{ option_fields.form_field_description[language.language_id] ? option_fields.form_field_description[language.language_id].placeholder }}" placeholder="{{ entry_placeholder }}" id="input-placeholder{{ form_row }}" class="form-control" />
              <div class="form-text">{{ help_placeholder }}</div>
              </div>  
            </div>
            <div class="row mb-3">
              <label class="col-sm-2 col-form-label" for="input-error_message{{ form_row }}">{{ entry_error }}</label>
              <div class="col-sm-10">
                <input type="text" name="option_fields[{{ form_row }}][form_fields][{{ language.language_id }}][error_message]" value="{{ option_fields.form_field_description[language.language_id] ? option_fields.form_field_description[language.language_id].error_message }}" placeholder="{{ entry_error }}" id="input-error_message{{ form_row }}" class="form-control" />
              <div class="form-text">{{ help_error }}</div>
              </div>  
            </div>
          </div>    
        {% endfor %}

            <div class="row mb-3">
              <label class="col-form-label col-sm-2" for="input-form_status">{{ entry_status }}</label>
              <div class="col-sm-10">
                <select name="option_fields[{{ form_row }}][form_status]" id="input-form_status" class="form-select">
                {% if option_fields.form_status %}
                <option value="1" selected="selected">{{ text_enable }}</option>
                <option value="0">{{ text_disable }}</option>
                {% else %}
                <option value="1">{{ text_enable }}</option>
                <option value="0" selected="selected">{{ text_disable }}</option>
                {% endif %}
                </select>
              </div>
            </div>


            <div class="row mb-3">
              <label class="col-sm-2 col-form-label" for="input-required">{{ entry_required }}</label>
              <div class="col-sm-10">
                <select name="option_fields[{{ form_row }}][required]" id="input-required" class="form-select">
                {% if option_fields.required %}
                <option value="1" selected="selected">{{ text_yes }}</option>
                <option value="0">{{ text_no }}</option>
                {% else %}
                <option value="1">{{ text_yes }}</option>
                <option value="0" selected="selected">{{ text_no }}</option>
                {% endif %}
                </select>
              </div>
            </div>

            
               <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-sortorder{{ form_row }}">{{ entry_sortorder }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="option_fields[{{ form_row }}][sort_order]" value="{{ option_fields.sort_order }}" placeholder="{{ entry_sortorder }}" id="input-sortorder{{ form_row }}" class="form-control" />
                  </div>  
                </div>
               <div class="row mb-3">  
                  <label class="col-sm-2 col-form-label" for="input-type">{{ entry_type }}</label>      
                  <div class="col-sm-10">           
                  <select name="option_fields[{{ form_row }}][type]" id="input-type[{{ form_row }}]" class="form-select typeoptions" rel="{{ form_row }}">                  
                  <optgroup label="{{ text_choose }}">
                  {% if option_fields.type == 'select' %}   <option value="select" selected="selected">{{ text_selects }}</option>
                  {% else %}                
                    <option value="select">{{ text_selects }}</option>
                  {% endif %}
                  {% if option_fields.type == 'radio' %}    <option value="radio" selected="selected">{{ text_radio }}</option> 
                  {% else %}               
                    <option value="radio">{{ text_radio }}</option> 
                  {% endif %}
                  {% if option_fields.type == 'checkbox' %}   <option value="checkbox" selected="selected">{{ text_checkbox }}</option>
                  {% else %}                
                    <option value="checkbox">{{ text_checkbox }}</option>
                  {% endif %}       
                  </optgroup>                    
                  <optgroup label="{{ text_input }}">
                  {% if option_fields.type == 'text' %} <option value="text" selected="selected">{{ text_text }}</option>
                  {% else %}
                  <option value="text">{{ text_text }}</option>
                  {% endif %}
                  {% if option_fields.type == 'textarea' %}
                    <option value="textarea" selected="selected">{{ text_textarea }}</option>
                  {% else %}
                    <option value="textarea">{{ text_textarea }}</option>
                  {% endif %}
                  {% if option_fields.type == 'number' %}
                    <option value="number" selected="selected">{{ text_number }}</option>
                  {% else %}
                    <option value="number">{{ text_number }}</option>
                  {% endif %}
                  {% if option_fields.type == 'telephone' %}
                    <option value="telephone" selected="selected">{{ text_telephone }}</option>
                  {% else %}
                    <option value="telephone">{{ text_telephone }}</option> 
                  {% endif %}
                  {% if option_fields.type == 'email' %}
                    <option value="email" selected="selected">{{ text_email }}</option>
                  {% else %}
                    <option value="email">{{ text_email }}</option> 
                  {% endif %}
                  {% if option_fields.type == 'emaile_exists' %}
                  <option value="emaile_exists" selected="selected">{{ text_emailexists }}</option>
                  {% else %}
                  <option value="emaile_exists">{{ text_emailexists }}</option> 
                  {% endif %}
                  {% if option_fields.type == 'password' %}
                  <option value="password" selected="selected">{{ text_password }}</option>
                  {% else %}
                  <option value="password">{{ text_password }}</option>
                  {% endif %}
                  {% if option_fields.type == 'confirm' %}
                  <option value="confirm" selected="selected">{{ text_cpassword }}</option>
                  {% else %}
                  <option value="confirm">{{ text_cpassword }}</option>
                  {% endif %}
              
                  </optgroup>        
                  <optgroup label="{{ text_file }}">  
                  {% if option_fields.type == 'file' %}
                  <option value="file" selected="selected">{{ text_file }}</option> 
                  {% else %}
                  <option value="file">{{ text_file }}</option> 
                  {% endif %}
                  </optgroup>
                  <optgroup label="{{ text_date }}">
                  {% if option_fields.type == 'date' %}
                  <option value="date" selected="selected">{{ text_date }}</option>
                  {% else %}
                  <option value="date">{{ text_date }}</option>
                  {% endif %}
                  {% if option_fields.type == 'time' %}
                  <option value="time" selected="selected">{{ text_time }}</option>
                  {% else %}
                  <option value="time">{{ text_time }}</option>
                  {% endif %}
                  {% if option_fields.type == 'datetime' %}
                  <option value="datetime" selected="selected">{{ text_datetime }}</option>
                  {% else %}
                  <option value="datetime">{{ text_datetime }}</option>
                  {% endif %}
                  </optgroup>
                  <optgroup label="{{ text_localisation }}">
                  {% if option_fields.type == 'country' %}
                  <option value="country" selected="selected">{{ text_country }}</option>
                  {% else %}
                  <option value="country">{{ text_country }}</option>
                  {% endif %}
                  {% if option_fields.type == 'zone' %}
                  <option value="zone" selected="selected">{{ text_zone }}</option>
                  {% else %}
                  <option value="zone">{{ text_zone }}</option>
                  {% endif %}
                  </optgroup>      
                  </select>                  
                </div>                         
              </div>
             <div class="row mb-3">
                <div class="col-sm-12">
                  <div class="table-responsive" id="form_option{{ form_row }}" 
                  {% if not option_fields.form_field_options %}
                  style="display:none"
                  {% endif %}>
                    <table id="tab-formfield{{ form_row }}" class="table table-striped table-bordered table-hover">
                    <thead>
                      <tr>
                      <td class="text-left">{{ entry_option_value }}</td>
                      <td class="text-left ">{{ entry_image }}</td>
                      <td class="text-left">{{ entry_price }}</td>
                      <td class="text-right">{{ entry_sort_order }}</td>
                      <td></td>
                      </tr>
                    </thead>
                    <tbody>
                                                        
                      {% if option_fields.form_field_options %}
                        {% for option_type in option_fields.form_field_options %}
                        <tr id="form_option-row-{{ form_option_row }}">  
                          <td class="text-left"><input type="hidden" name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][form_id]" value="" />
                          {% for language in languages %}
                          <div class="input-group">
                            <span class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}" /></span>
                            <input type="text" name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][option_value_description][{{ language.language_id }}][name]" value="{{ option_type.name[language.language_id] }}" placeholder="{{ entry_option_value }}" class="form-control" />
                          </div>
                          {% endfor %}
                          </td>

                          <td>
                            <div class="card image">
                              <img src="{{ option_type.thumb }}" alt="" title="" id="product-image-{{ form_option_row }}" data-oc-placeholder="{{ placeholder }}" class="card-img-top"/>
                              <input type="hidden" name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][image]" value="{{ option_type.image }}" id="input-image-{{ form_option_row }}"/>

                              <div class="card-body">
                                <button type="button" data-oc-toggle="image" data-oc-target="#input-image-{{ form_option_row }}" data-oc-thumb="#product-image-{{ form_option_row }}" class="btn btn-primary btn-block"><i class="fa-solid fa-pencil"></i> {{ button_edit }}</button>
                                <button type="button" data-oc-toggle="clear" data-oc-target="#input-image-{{ form_option_row }}" data-oc-thumb="#product-image-{{ form_option_row }}" class="btn btn-warning btn-block"><i class="fa-regular fa-trash-can"></i> {{ button_clear }}</button>
                              </div>
                            </div>
                          </td>

                          <td class="text-right"><select name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][price_prefix]" class="form-control">
                              {% if option_type.price_prefix == '+' %}
                              <option value="+" selected="selected">+</option>
                              {% else %}
                              <option value="+">+</option>
                              {% endif %}
                              {% if option_type.price_prefix == '-' %}
                              <option value="-" selected="selected">-</option>
                              {% else %}
                              <option value="-">-</option>
                              {% endif %}
                              {% if option_type.price_prefix == '+%' %}
                              <option value="+%" selected="selected">+%</option>
                              {% else %}
                              <option value="+%">+%</option>
                              {% endif %}
                              {% if option_type.price_prefix == '-%' %}
                              <option value="-%" selected="selected">-%</option>
                              {% else %}
                              <option value="-%">-%</option>
                              {% endif %}
                            </select>
                            <input type="text" name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][price]" value="{{ option_type.price }}" placeholder="{{ entry_price }}" class="form-control" />
                          </td>

                          <td class="text-right"><input type="text" name="option_fields[{{ form_row }}][option_type][{{ form_option_row }}][sort_order]"  value="{{ option_type['sort_order'] }}" placeholder="{{ entry_sort_order }}" class="form-control" /></td>
                          <td class="text-left"><button type="button" onclick="$('#form_option-row-{{ form_option_row }}').remove();" data-oc-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa fa-minus-circle"></i></button></td>
                        </tr>
                      {% set form_option_row = form_option_row + 1 %}
                      {% endfor %}
                      {% endif %}
                      </tbody>
                      <tfoot>
                        <tr>
                          <td colspan="4"></td>
                          <td class="text-left"><button type="button" onclick="addFormOption('{{ form_row }}');" data-bs-toggle="tooltip" title="{{ button_option_add }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i></button></td>
                        </tr>
                      </tfoot>
                    </table>
                  </div>  
                
              </div>      
              </div>

            </div>
          </div>
            
        {% set form_row = form_row + 1 %}
        {% endfor %}
        {% endif %}
          </div>
        </div>
       </div>
      </div>
<!-- pending  end -->

            <div id="tab-link" class="tab-pane">
              
              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_display }}</label>
                <div class="col-sm-10">
                  <select name="display_type" id="input-display_type" class="form-select">
                    <option value="">{{ text_select }}</option>
                    {% for result in displaytypes %}
                    {% if display_type == result.display_type %}
                      <option value="{{ result.display_type }}" selected="selected">{{ result.value }}</option> 
                    {% else %}
                      <option value="{{ result.display_type }}">{{ result.value }}</option> 
                    {% endif %}
                    {% endfor %}
                  </select>
                </div>
              </div> 

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_custgroup }}</label>
                <div class="col-sm-10">
                  <div class="form-control" style="height: 150px; overflow: auto;">
                    {% for customergroup in customergroups %}
                   <div class="checkbox">
                    <label> {% if customergroup.customer_group_id in form_customer %}
                        <input type="checkbox" name="form_customer[]" value="{{ customergroup.customer_group_id }}" checked="checked"/>
                        {{ customergroup.name }}
                      {% else %}
                        <input type="checkbox" name="form_customer[]" value="{{ customergroup.customer_group_id }}"/>
                        {{ customergroup.name }}
                      {% endif %} </label>
                  </div>
                    {% endfor %}
                  </div>
                </div>
              </div> 

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_store }}</label>
                <div class="col-sm-10">
                 <div class="form-control" style="height: 150px; overflow: auto;"> {% for store in stores %}
                    <div class="checkbox">
                      <label> {% if store.store_id in form_store %}
                        <input type="checkbox" name="form_store[]" value="{{ store.store_id }}" checked="checked" />
                        {{ store.name }}
                        {% else %}
                        <input type="checkbox" name="form_store[]" value="{{ store.store_id }}" />
                        {{ store.name }}
                        {% endif %}</label>
                    </div>
                    {% endfor %}
                  </div>
                </div>
              </div> 

              <div class="row mb-3">
                <label for="input-parent" class="col-sm-2 col-form-label">{{ entry_assignproduct }}</label>
                <div class="col-sm-10">
                  <select name="assign_product" id="input-assign-product" class="form-select">
                    <option value="">{{ text_select }}</option>
                    {% for result in assigns %}
                      {% if assign_product == result.value %}
                        <option value="{{ result.value }}" selected="selected">{{ result.assign_product }}</option> 
                      {% else %}
                        <option value="{{ result.value }}">{{ result.assign_product }}</option> 
                      {% endif %}
                    {% endfor %}
                  </select>

                </div>
              </div>               

          <div id="3" class="row mb-3 colors" style="display:none;">
            <label class="col-sm-2 col-form-label" for="input-product">{{ entry_product }}</label>
            <div class="col-sm-10">
			          {% if(VERSION>='4.0.2.0') %}
		              <input type="text" name="product" value="" placeholder="{{ entry_product }}" id="input-product" data-oc-target="autocomplete-product" class="form-control" autocomplete="off">
		              <ul id="autocomplete-product" class="dropdown-menu"></ul>
		            {% else %}
                  <input type="text" name="product" value="" placeholder="{{ entry_product }}" id="input-product"  list="list-product" class="form-control"/>
                  <datalist id="list-product"></datalist>
				        {% endif%}
                 <div class="input-group">
                  <div class="form-control p-0" style="height: 150px; overflow: auto;">
                    <table id="form-product" class="table table-sm m-0">
                      <tbody>
                        {% if products %}
                        {% for form_product in products %}
                          <tr id="form-product-{{ form_product.product_id }}">
                            <td>{{ form_product.name }}<input type="hidden" name="form_product[]" value="{{ form_product.product_id }}"/></td>
                            <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>
                          </tr>
                        {% endfor %}
                        {% endif %}  
                      </tbody>
                    </table>
                  </div>                  
                </div>                
            <div class="form-text">{{ help_category }}</div>
            </div>
          </div>

<!-- category -->
            <div class="row mb-3">
              <label class="col-sm-2 col-form-label">{{ entry_category }}</label>
              <div class="col-sm-10">
			  {% if(VERSION>='4.0.2.0') %}
		              <input type="text" name="category" value="" placeholder="{{ entry_category }}" id="input-category" data-oc-target="autocomplete-category" class="form-control" autocomplete="off">
		              <ul id="autocomplete-category" class="dropdown-menu"></ul>
		            {% else %}
                <input type="text" name="category" value="" placeholder="{{ entry_category }}" id="input-category" list="list-category" class="form-control"/>
                <datalist id="list-category"></datalist>
				 {% endif %}  
                <div class="input-group">
                  <div class="form-control p-0" style="height: 150px; overflow: auto;">
                    <table id="form-category" class="table table-sm m-0">
                      <tbody>
                        {% for form_category in form_categoriess %}
                          <tr id="form-category-{{ form_category.category_id }}">
                            <td>{{ form_category.name }}<input type="hidden" name="form_category[]" value="{{ form_category.category_id }}"/></td>
                            <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>
                          </tr>
                        {% endfor %}
                      </tbody>
                    </table>
                  </div>
                  
                </div>
                <div class="form-text">{{ help_category }}</div>
              </div>
            </div>

            <!--  -->
            <!-- manufecturer -->

             <div class="row mb-3">
                <label class="col-sm-2 col-form-label">{{ entry_manufacturer}}</label>
                <div class="col-sm-10">
				{% if(VERSION>='4.0.2.0') %}
		              <input type="text" name="manufacturer" value="" placeholder="{{ entry_manufacturer }}" id="input-manufacturer" data-oc-target="autocomplete-manufacturer" class="form-control" autocomplete="off">
		              <ul id="autocomplete-manufacturer" class="dropdown-menu"></ul>
		            {% else %}
                  <input type="text" name="manufacturer" value="" placeholder="{{ entry_manufacturer }}" id="input-manufacturer" list="list-manufacturer" class="form-control"/>
                  <datalist id="list-manufacturer"></datalist>
				  {% endif%}
                  <div class="input-group">
                    <div class="form-control p-0" style="height: 150px; overflow: auto;">
                      <table id="form-manufacturer" class="table table-sm m-0">
                        <tbody>
                          {% for form_manufacturer in form_manufacturers %}
                            <tr id="form-manufacturer-{{ form_manufacturer.manufacturer_id }}">
                              <td>{{ form_manufacturer.name }}<input type="hidden" name="form_manufacturer[]" value="{{ form_manufacturer.manufacturer_id }}"/></td>
                              <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>
                            </tr>
                          {% endfor %}
                        </tbody>
                      </table>
                    </div>
                    
                  </div>
                  <div class="form-text">{{ help_manufacturer }}</div>
                </div>
              </div>
<!--  -->
              <div class="row mb-3">
                <label class="col-sm-2 col-form-label">{{ entry_information }}</label>
                <div class="col-sm-10">
                  {% for result in informations %}
                  <div class="checkbox">
                    <label>
                      {% if result.information_id in information %}
                      <input type="checkbox" name="information[]" value="{{ result.information_id }}" checked="checked" />
                      {{ result.title }}
                      {% else %}
                      <input type="checkbox" name="information[]" value="{{ result.information_id }}" />
                      {{ result.title }}
                      {% endif %}
                    </label>
                  </div>
                  {% endfor %}
                </div>
              </div>
            </div>

          <div id="tab-notify" class="tab-pane">
            <ul class="nav nav-tabs">
              <li class="nav-item"><a href="#tab-customeremail" data-bs-toggle="tab" class="nav-link active">{{ text_custemail }}</a></li>
              <li class="nav-item"><a href="#tab-adminemail" data-bs-toggle="tab" class="nav-link">{{ text_adminemail }}</a></li>
            </ul>
          <div class="tab-content">
            <div id="tab-customeremail" class="tab-pane active">
              <ul class="nav nav-tabs customer_language" id="language">
                {% for language in languages %}
                  <li class="nav-item"><a href="#customer_language{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/> {{ language.name }}</a></li>
                {% endfor %}
              </ul>
              <div class="tab-content">
                {% for language in languages %}
                  <div id="customer_language{{ language.language_id }}" class="tab-pane{% if loop.first %} active{% endif %}">

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-customer_notification{{ language.language_id }}">{{ entry_notification }}</label>
                    <div class="col-sm-10">
                      <select name="form_notification[{{ language.language_id }}][customer_notification]" id="input-customer_notification{{ language.language_id }}" class="form-control">
                          {% if form_notification[language.language_id]['customer_notification'] == 1 %}
                              <option value="1" selected="selected">{{ text_yes }}</option>
                              <option value="0">{{ text_no }}</option>
                          {% else %}
                              <option value="1">{{ text_yes }}</option>
                              <option value="0" selected="selected">{{ text_no }}</option>
                          {% endif %}
                      </select>
                    </div>
                  </div>


                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label control-label" for="input-customer_subject{{ language.language_id }}">{{ entry_subject }}</label>
                    <div class="col-sm-10">
                   <input type="text" name="form_notification[{{ language.language_id }}][customer_subject]" placeholder="{{ entry_subject }}" value="{{ form_notification[language.language_id] ? form_notification[language.language_id].customer_subject }}" id="input-customer_subject{{ language.language_id }}" class="form-control"/>
                  </div>
                </div>

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label control-label" for="input-customer_message{{ language.language_id }}">{{ entry_message }}</label>
                    <div class="col-sm-10">
                      <textarea name="form_notification[{{ language.language_id }}][customer_message]" placeholder="{{ entry_message }}" value="" id="input-customer_message{{ language.language_id }}" data-oc-toggle="ckeditor" data-lang="{{ ckeditor }}" class="form-control">{{ form_notification[language.language_id] ? form_notification[language.language_id].customer_message }}</textarea>
                    </div>
                  </div>


                  </div>
                {% endfor %}
              </div>
            </div>
  
          <div id="tab-adminemail" class="tab-pane" >
            <ul class="nav nav-tabs" id="language">
              {% for language in languages %}
              <li class="nav-item"><a href="#admin_language-{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/> {{ language.name }}</a></li>
              {% endfor %}
            </ul>
            <div class="tab-content">
              {% for language in languages %}
               <div id="admin_language-{{ language.language_id }}" class="tab-pane{% if loop.first %} active{% endif %}">

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-admin_notification{{ language.language_id }}">{{ entry_notification }}</label>
                  <div class="col-sm-10">
                    <select name="form_notification[{{ language.language_id }}][admin_notification]" id="input-admin_notification{{ language.language_id }}" class="form-control">
                      {% if form_notification[language.language_id]['admin_notification'] == 1 %}
                          <option value="1" selected="selected">{{ text_yes }}</option>
                          <option value="0">{{ text_no }}</option>
                      {% else %}
                          <option value="1">{{ text_yes }}</option>
                          <option value="0" selected="selected">{{ text_no }}</option>
                      {% endif %}
                    </select>
                  </div>
                </div>

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-admin_subject{{ language.language_id }}">{{ entry_subject }}</label>
                  <div class="col-sm-10">
                    <input type="text" name="form_notification[{{ language.language_id }}][admin_subject]" placeholder="{{ entry_subject }}" value="{{ form_notification[language.language_id] ? form_notification[language.language_id].admin_subject }}" id="input-admin_subject{{ language.language_id }}" class="form-control"/>
                    
                  </div>
                </div>
                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-admin_message{{ language.language_id }}">{{ entry_message }}</label>
                  <div class="col-sm-10">
                    <textarea name="form_notification[{{ language.language_id }}][admin_message]" placeholder="{{ entry_message }}" value="" id="input-admin_message{{ language.language_id }}"data-oc-toggle="ckeditor" data-lang="{{ ckeditor }}" class="form-control">{{ form_notification[language.language_id] ? form_notification[language.language_id].admin_message }}</textarea>
                  </div>
                </div>
              </div>
              {% endfor %}
            </div>
          </div>
            <div class="row mb-3">
              <label class="col-sm-2 col-form-label" for="input-shortcut">{{ entry_shortcut }}</label>
              <div class="col-sm-10" style="padding-top: 9px;font-size:14px;">
               {{ text_shortcut }}
              </div>
            </div>

          </div>
        </div>

          <div id="tab-success" class="tab-pane">
              <ul class="nav nav-tabs customer_language" id="language">
                {% for language in languages %}
                  <li class="nav-item"><a href="#customer_language-{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/> {{ language.name }}</a></li>
                {% endfor %}
              </ul>
              <div class="tab-content">
                {% for language in languages %}
                  <div id="customer_language-{{ language.language_id }}" class="tab-pane{% if loop.first %} active{% endif %}">

                <div class="row mb-3">
                  <label class="col-sm-2 col-form-label" for="input-metatitle">{{ entry_metatitle }}</label>
                    <div class="col-sm-10">
                      <input type="text" name="succes_form[{{ language.language_id }}][success_meta_title]" placeholder="{{ entry_metatitle }}" value="{{ succes_form[language.language_id] ? succes_form[language.language_id].success_meta_title }}" id="input-success_meta_title{{ language.language_id }}" class="form-control"/>
                    </div>
                  </div>

                  <div class="row mb-3">
                    <label class="col-sm-2 col-form-label control-label" for="input-title{{ language.language_id }}">{{ entry_title }}</label>
                      <div class="col-sm-10">
                       <input name="succes_form[{{ language.language_id }}][success_title]" value="{{ succes_form[language.language_id] ? succes_form[language.language_id].success_title }}" placeholder="{{ entry_title }}" id="input-success_title{{ language.language_id }}" class="form-control"/>
                        {% if error_title[language.language_id] %}
                          <div class="text-danger">{{ error_title[language.language_id]}}</div>
                        {% endif %}
                    </div>
                  </div>

                  <div class="row mb-3">
                    <label class="col-sm-2 col-form-label control-label" for="input-success_description{{ language.language_id }}">{{ entry_descriptionss }}</label>
                    <div class="col-sm-10">
                      <textarea name="succes_form[{{ language.language_id }}][success_description]" placeholder="{{ entry_descriptionss }}" value="" id="input-success_description{{ language.language_id }}" data-oc-toggle="ckeditor" data-lang="{{ ckeditor }}" class="form-select">{{ succes_form[language.language_id] ? succes_form[language.language_id].success_description }}</textarea>

                          {% if master_id %}
                            <div class="input-group-text">
                              <div class="form-check form-switch">
                                <input type="checkbox" name="override[succes_form][{{ language.language_id }}][name]" value="1" id="input-variant-name-{{ language.language_id }}" data-oc-toggle="switch" data-oc-target="#input-name-{{ language.language_id }}" class="form-check-input"/> <label for="input-variant-name-{{ language.language_id }}" class="form-check-label"></label>
                              </div>
                            </div>
                          {% endif %}
                          <div id="error-name-{{ language.language_id }}" class="invalid-feedback"></div>

                    </div>
                  </div>

                  </div>
                {% endfor %}
              </div>
            </div>

            <div id="tab-seo" class="tab-pane">
              <div class="alert alert-info"><i class="fas fa-info-circle"></i> {{ text_keyword }}</div>
              <div class="table-responsive">
                <table class="table table-bordered table-hover">
                  <thead>
                    <tr>
                      <td class="text-start">{{ entry_store }}</td>
                      <td class="text-start">{{ entry_keyword }}</td>
                    </tr>
                  </thead>
                  <tbody>
                    {% for store in stores %}
                      <tr>
                        <td class="text-start">{{ store.name }}</td>
                        <td class="text-start">{% for language in languages %}
                            <div class="input-group">
                              <div class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/></div>
                              <input type="text" name="form_seo_url[{{ store.store_id }}][{{ language.language_id }}]" value="{% if form_seo_url[store.store_id][language.language_id] %}{{ form_seo_url[store.store_id][language.language_id] }}{% endif %}" placeholder="{{ entry_keyword }}" id="input-keyword-{{ store.store_id }}-{{ language.language_id }}" class="form-control"/>
                            </div>
                            <div id="error-keyword-{{ store.store_id }}-{{ language.language_id }}" class="invalid-feedback"></div>
                          {% endfor %}</td>
                      </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>

               <div class="table-responsive">
                <table class="table table-bordered table-hover">
                  <thead>
                    <tr>
                      <td class="text-start">{{ entry_store }}</td>
                      <td class="text-start">{{ entry_success_page_seo }}</td>
                    </tr>
                  </thead>
                  <tbody>
                    {% for store in stores %}
                      <tr>
                        <td class="text-start">{{ store.name }}</td>
                        <td class="text-start">{% for language in languages %}
                            <div class="input-group">
                              <div class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}"/></div>
                              <input type="text" name="form_success_page_seo[{{ store.store_id }}][{{ language.language_id }}]" value="{% if form_success_page_seo[store.store_id][language.language_id] %}{{ form_success_page_seo[store.store_id][language.language_id] }}{% endif %}" placeholder="{{ entry_keyword }}" id="input-keyword-{{ store.store_id }}-{{ language.language_id }}" class="form-control"/>
                            </div>
                            <div id="error-keyword-{{ store.store_id }}-{{ language.language_id }}" class="invalid-feedback"></div>
                          {% endfor %}</td>
                      </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
            </div> 

            <div id="tab-custome" class="tab-pane">
              <div class="row mb-3">
                <label class="col-sm-2 col-form-label control-label"   for="input-custome_style">{{ entry_customestyle }}</label>
            <div class="col-sm-10">
              <textarea name="custome_style" rows="10" placeholder="{{ entry_customestyle }}" id="input-custome_style" class="form-control">{{ custome_style }}</textarea>
            </div>
          </div>
            </div>


          </div>
          <input type="hidden" name="form_id" value="{{ form_id }}" id="input-category-id"/></form>
      </div>
    </div>
  </div>
</div>
 
  
<script type="text/javascript"><!--

  $('textarea[data-oc-toggle=\'ckeditor\']').ckeditor();


var form_option_row = '{{ form_option_row }}';

var form_row = '{{ form_row }}';

function addFormfield() {
  html  = ' <div class="tab-pane active" id="tab-formfield' + form_row + '">';
  html += ' <ul class="nav nav-tabs fieldslanguage' + form_row + '" id="forms_fields' + form_row + '">';
  {% for language in languages %}
  html += ' <li class="nav-item"><a href="#forms_fields_' + form_row + '_{{ language.language_id }}" data-bs-toggle="tab" class="nav-link{% if loop.first %} active{% endif %}"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}" /> {{ language.name }}</a></li>';
  {% endfor %}
  html += ' </ul>';
  html += '<div class="tab-content">';
  {% for language in languages %}
  html += '<div class="tab-pane{% if loop.first %} active{% endif %} fields fieldslanguage{{ language.language_id }}" id="forms_fields_' + form_row + '_{{ language.language_id }}">';
  html += '  <input type="hidden" name="option_fields[' + form_row + '][form_id]" value="" />';
  html += '<div class="row mb-3">';
  html += '    <label class="col-sm-2 col-form-label" for="input-field_name{{ language.language_id }}' + form_row + '"><span data-bs-toggle="tooltip" title="{{ help_fieldname }}">{{ entry_fieldname }}</span></label><div class="col-sm-10">';                     
  html += '    <input type="text" name="option_fields[' + form_row + '][form_fields][{{ language.language_id }}][field_name]" value="" id="input-fieldname' + form_row + '" placeholder="{{ entry_fieldname }}" class="form-control"><div class="form-text">{{ help_fieldname }}</div> </div>';
    html += ' </div>';
      
  html += '<div class="row mb-3">';
  html += '    <label class="col-sm-2 col-form-label" for="input-help_text{{ language.language_id }}' + form_row + '"><span data-bs-toggle="tooltip" title="{{ help_helptext }}">{{ entry_helptext }}</span></label><div class="col-sm-10">';                    
  html += '    <input type="text" name="option_fields[' + form_row + '][form_fields][{{ language.language_id }}][help_text]" value="" id="input-help_text{{ language.language_id }}' + form_row + '" placeholder="{{ entry_helptext }}" class="form-control"><div class="form-text">{{ help_helptext }}</div></div>';
    html += ' </div>';
  
  html += '<div class="row mb-3">';
  html += '    <label class="col-sm-2 col-form-label" for="input-placeholder{{ language.language_id }}' + form_row + '"><span data-bs-toggle="tooltip" title="{{ help_placeholder }}">{{ entry_placeholder }}</span></label><div class="col-sm-10">';                    
  html += '    <input type="text" name="option_fields[' + form_row + '][form_fields][{{ language.language_id }}][placeholder]" value="" id="input-placeholder{{ language.language_id }}' + form_row + '" placeholder="{{ entry_placeholder }}" class="form-control"><div class="form-text">{{ help_placeholder }}</div></div>';
    html += ' </div>';                     
    
  html += '<div class="row mb-3">';
  html += '   <label class="col-sm-2 col-form-label" for="input-error_message{{ language.language_id }}' + form_row + '"><span data-bs-toggle="tooltip" title="{{ help_error }}">{{ entry_error }}</span></label> <div class="col-sm-10">';     
// pending

  html += '    <input type="text" name="option_fields[' + form_row + '][form_fields][{{ language.language_id }}][error_message]" value="" id="input-error_message{{ language.language_id }}' + form_row + '" placeholder="{{ entry_error }}" class="form-control"><div class="form-text">{{ help_error }}</div></div>';
    html += ' </div>';                     
    html += ' </div>';                     
  {% endfor %}  
  html += '<div class="row mb-3">';                    
  html += '   <label  class="col-sm-2 col-form-label" for="input-form_status' + form_row + '">{{ entry_status }}</label>';
  html += ' <div class="col-sm-10">';                    
                       
  html += '   <select name="option_fields[' + form_row + '][form_status]" id="input-form_status" class="form-select">';
            {% if form_status %}                     
  html += '     <option value="1" selected="selected">{{ text_enable }}</option>';                     
  html += '     <option value="0">{{ text_disable }}</option>';
            {% else %}             
  html += '     <option value="1">{{ text_enable }}</option>';                     
  html += '     <option value="0" selected="selected">{{ text_disable }}</option>';
            {% endif %}       
  html += '   </select>';                    
  html += ' </div>';                     
  html += '</div>';
                       
  html += '<div class="row mb-3">';                    
  html += '   <label class="col-sm-2 col-form-label" for="input-required' + form_row + '"><span data-bs-toggle="tooltip" title="{{ help_required }}">{{ entry_required }}</span></label>';html += ' <div class="col-sm-10">';                    
                       
  html += '   <select name="option_fields[' + form_row + '][required]" id="input-required" class="form-select">';
            {% if required %}                    
  html += '     <option value="1" selected="selected">{{ text_yes }}</option>';                    
  html += '     <option value="0">{{ text_no }}</option>';
            {% else %}             
  html += '     <option value="1">{{ text_yes }}</option>';                    
  html += '     <option value="0" selected="selected">{{ text_no }}</option>';
            {% endif %}       
  html += '   </select>';                    
  html += ' </div>';                     
  html += '</div>';                    
  

  html += '<div class="row mb-3">';
  html += ' <label class="col-sm-2 col-form-label" for="input-sortorder{{ language.language_id }}' + form_row + '">{{ entry_sortorder }}</label><div class="col-sm-10">';                     
  html += '    <input type="text" name="option_fields[' + form_row + '][sort_order]" value="" id="input-sortorder' + form_row + '" placeholder="{{ entry_sortorder }}" class="form-control"></div>';
    html += ' </div>';  
// option type//
                       
  html += '<div class="row mb-3">'; 
  html += '   <label class="col-sm-2 col-form-label" for="input-type">{{ entry_type }}</label>';html += '  <div class="col-sm-10">';                    
  html += '   <select name="option_fields[' + form_row + '][type]" id="input-type' + form_row + '" class="form-select typeoptions" rel="'+form_row+'">';                    
  html += '     <optgroup label="{{ text_choose }}">';
            {% if type == 'select' %}          
  html += '     <option value="select" selected="selected">{{ text_selects }}</option>';
            {% else %}                
  html += '     <option value="select">{{ text_selects }}</option>';
            {% endif %}
            {% if type == 'radio' %}        
  html += '     <option value="radio" selected="selected">{{ text_radio }}</option>'; 
            {% else %}               
  html += '     <option value="radio">{{ text_radio }}</option>'; 
            {% endif %}
            {% if type == 'checkbox' %}       
  html += '     <option value="checkbox" selected="selected">{{ text_checkbox }}</option>';
            {% else %}                
  html += '     <option value="checkbox">{{ text_checkbox }}</option>';
            {% endif %}       
  html += '     </optgroup>';                    
  html += '     <optgroup label="{{ text_input }}">';
            {% if type == 'text' %}        
  html += '     <option value="text" selected="selected">{{ text_text }}</option>';
            {% else %}
  html += '     <option value="text">{{ text_text }}</option>';
            {% endif %}
            {% if type == 'textarea' %}
  html += '     <option value="textarea" selected="selected">{{ text_textarea }}</option>';
            {% else %}
  html += '     <option value="textarea">{{ text_textarea }}</option>';
            {% endif %}
            {% if type == 'number' %}
  html += '     <option value="number" selected="selected">{{ text_number }}</option>';
            {% else %}
  html += '     <option value="number">{{ text_number }}</option>';
            {% endif %}
            {% if type == 'telephone' %}
  html += '     <option value="telephone" selected="selected">{{ text_telephone }}</option>';
            {% else %}
  html += '     <option value="telephone">{{ text_telephone }}</option>'; 
            {% endif %}
            {% if type == 'email' %}
  html += '     <option value="email" selected="selected">{{ text_email }}</option>';
            {% else %}
  html += '     <option value="email">{{ text_email }}</option>'; 
            {% endif %}
            {% if type == 'emaile_exists' %}
  html += '     <option value="emaile_exists" selected="selected">{{ text_emailexists }}</option>';
            {% else %}
  html += '     <option value="emaile_exists">{{ text_emailexists }}</option>'; 
            {% endif %}
            {% if type == 'password' %}
  html += '     <option value="password" selected="selected">{{ text_password }}</option>';
            {% else %}
  html += '     <option value="password">{{ text_password }}</option>';
            {% endif %}
            {% if type == 'confirm' %}
  html += '     <option value="confirm" selected="selected">{{ text_cpassword }}</option>';
            {% else %}
  html += '     <option value="confirm">{{ text_cpassword }}</option>';
            {% endif %}
                   
  html += '     </optgroup>';        
  html += '     <optgroup label="{{ text_file }}">';  
            {% if type == 'file' %}
  html += '     <option value="file" selected="selected">{{ text_file }}</option>'; 
            {% else %}
  html += '     <option value="file">{{ text_file }}</option>'; 
            {% endif %}
  html += '     </optgroup>';
            
  html += '     <optgroup label="{{ text_date }}">';
            {% if type == 'date' %}
  html += '     <option value="date" selected="selected">{{ text_date }}</option>';
            {% else %}
  html += '     <option value="date">{{ text_date }}</option>';
            {% endif %}
            {% if type == 'time' %}
  html += '     <option value="time" selected="selected">{{ text_time }}</option>';
            {% else %}
  html += '     <option value="time">{{ text_time }}</option>';
            {% endif %}
            {% if type == 'datetime' %}
  html += '     <option value="datetime" selected="selected">{{ text_datetime }}</option>';
            {% else %}
  html += '     <option value="datetime">{{ text_datetime }}</option>';
            {% endif %}
  html += '     </optgroup>';
  html += '     <optgroup label="{{ text_localisation }}">';
            {% if type == 'country' %}
  html += '     <option value="country" selected="selected">{{ text_country }}</option>';
            {% else %}
  html += '     <option value="country">{{ text_country }}</option>';
            {% endif %}
            {% if type == 'zone' %}
  html += '     <option value="zone" selected="selected">{{ text_zone }}</option>';
            {% else %}
  html += '     <option value="zone">{{ text_zone }}</option>';
            {% endif %}
  html += '     </optgroup>';      
  html += '   </select>';                    
  html += ' </div>';                     
  html += '</div>'; 
    
  // Select Option//
                       
  html += '  <div class="row mb-3">';
  html += '   <div class="col-sm-12">';
  html += '   <div class="table-responsive" id="form_option'+form_row+'">';
  html += '   <table id="tab-formfield'+form_row+'" class="table table-striped table-bordered table-hover">';
  html += '   <thead>';
  html += '   <tr>';
  html += '   <td class="text-left">{{ entry_option_value }}</td>';
  html += '   <td class="text-left">{{ entry_image }}</td>';
  html += '   <td class="text-left">{{ entry_price }}</td>';
  html += '   <td class="text-right">{{ entry_sort_order }}</td>';
  html += '   <td></td>';
  html += '   </tr>';
  html += '   </thead>';
  html += '   <tbody>';
      
  html += '   <tr id="form_option-row-{{ form_option_row }}">';
    
  html += '   </tr>';
      
  html += '   </tbody>';
  html += '   <tfoot>';
  html += '   <tr>';
  html += '   <td colspan="4"></td>';
  html += '   <td class="text-left"><button type="button" onclick="addFormOption('+form_row+');" data-bs-toggle="tooltip" title="{{ button_option_add }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i></button></td>';
  html += '   </tr>';
  html += '   </tfoot>';
  html += '   </table>';
  html += '   </div>';
  html += '   </div>';
  html += '   </div></div>';                     
                       
// option type//
  html += ' </div>';
  
    $('#tab-formfield .one').append(html);
    $('.fieldslanguage'+form_row+' a:first').tab('show');
    
  $('#formfield-add').before('<li><a href="#tab-formfield' + form_row + '" data-bs-toggle="tab"><i class="fa fa-minus-circle" onclick="$(\'#formfield a:first\').tab(\'show\'); $(\'a[href=\\\'#tab-formfield' + form_row + '\\\']\').parent().remove(); $(\'#tab-formfield' + form_row + '\').remove();"></i> {{ tab_option }} ' + form_row + '</a></li>');

  $('#formfield a[href=\'#tab-formfield' + form_row + '\']').tab('show');
    
  $('#tab-formfield' + form_row + ' .form-group[data-sort]').detach().each(function() {
    if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('#tab-formfield' + form_row + ' .form-group').length) {
      $('#tab-formfield' + form_row + ' .form-group').eq($(this).attr('data-sort')).before(this);
    }

    if ($(this).attr('data-sort') > $('#tab-formfield' + form_row + ' .form-group').length) {
      $('#tab-formfield' + form_row + ' .form-group:last').after(this);
    }

    if ($(this).attr('data-sort') < -$('#tab-formfield' + form_row + ' .form-group').length) {
      $('#tab-formfield' + form_row + ' .form-group:first').before(this);
    }
  });

  form_row++;
    
}
//--></script>
  
<script type="text/javascript"><!--
$('body').delegate('.typeoptions', 'click', function(e) {
  rel=$(this).attr('rel');
  if (this.value == 'select' || this.value == 'radio' || this.value == 'checkbox' || this.value == 'image') {
    $('#form_option'+rel).show();
  } else {
    $('#form_option'+rel).hide();
  }
});
  
function addFormOption(mainid) {
  html  = '<tr id="form_option-row' + form_option_row + '">'; 
    html += '  <td class="text-left"><input type="hidden" name="option_fields[' + mainid + '][option_type][' + form_option_row + '][form_id]" value="" />';
  {% for language in languages %}
  html += '    <div class="input-group">';
  html += '      <span class="input-group-text"><img src="language/{{ language.code }}/{{ language.code }}.png" title="{{ language.name }}" /></span><input type="text" name="option_fields[' + mainid + '][option_type][' + form_option_row + '][option_value_description][{{ language.language_id }}][name]" value="" placeholder="{{ entry_option_value }}" class="form-control" />';
    html += '    </div>';
  {% endfor %}
  html += '  </td>';

// 
  html += '  <td><div class="card image">';
  html += '    <img src="{{ placeholder|escape('js') }}" alt="" title="" id="thumb-image-' + form_option_row + '" data-oc-placeholder="{{ placeholder|escape('js') }}" class="card-img-top"/> <input type="hidden" name="option_fields[' + mainid + '][option_type][' + form_option_row + '][image]" value="" id="input-image-' + form_option_row + '"/>';
  html += '    <div class="card-body">';
  html += '      <button type="button" data-oc-toggle="image" data-oc-target="#input-image-' + form_option_row + '" data-oc-thumb="#thumb-image-' + form_option_row + '" class="btn btn-primary btn-sm btn-block"><i class="fa-solid fa-pencil"></i> {{ button_edit|escape('js') }}</button>';
  html += '      <button type="button" data-oc-toggle="clear" data-oc-target="#input-image-' + form_option_row + '" data-oc-thumb="#thumb-image-' + form_option_row + '" class="btn btn-warning btn-sm btn-block"><i class="fa-regular fa-trash-can"></i> {{ button_clear|escape('js') }}</button>';
  html += '    </div>';
  html += '  </div></td>';
// 
  html += '  <td class="text-right"><select name="option_fields[' + mainid + '][option_type][' + form_option_row + '][price_prefix]" class="form-control">';
  html += '    <option value="+">+</option>';
  html += '    <option value="-">-</option>';
  html += '    <option value="+%">+%</option>';
  html += '    <option value="-%">-%</option>';
  html += '  </select>';
  html += '  <input type="text" name="option_fields[' + mainid + '][option_type][' + form_option_row + '][price]" value="" placeholder="{{ entry_price }}" class="form-control" /></td>';


  html += '  <td class="text-right"><input type="text" name="option_fields[' + mainid + '][option_type][' + form_option_row + '][sort_order]" value="" placeholder="{{ entry_sort_order }}" class="form-control" /></td>';
  html += '  <td class="text-left"><button type="button" onclick="$(\'#form_option-row' + form_option_row + '\').remove();" data-bs-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa fa-minus-circle"></i></button></td>';
  html += '</tr>';  
  
  $('#form_option'+mainid+' tbody').append(html);
  
  form_option_row++;
} 
  
</script> 
  
 
 <script>
	$(document).ready(function(){
    	var selectedvalue = $('#input-assign-product').val();
		if(selectedvalue!=''){
			$('#'+selectedvalue).show();
		}
    });
    $(function() {
        $('#input-assign-product').change(function(){
            $('.colors').hide();
            $('#' + $(this).val()).show();
        });
		$('#input-assign-product option:selected').show();
    });

   

  // img delete
//$('.delete').click(function(){
$(document).on('click','.delete',function() {
  var field_id = $(this).attr('rel');
  $.ajax({
  url: 'index.php?route=extension/tmdadvanceformbuilder/tmd/form|fielddelete&user_token={{ user_token }}&field_id='+field_id,
  type:'post',
  dataType:'json',
    beforeSend: function() {
  },
  success: function(json) {
    
  }
});
});
  
</script> 

<script type="text/javascript">
  //Advanced product
$('input[name=\'form_product\']').autocomplete({
  'source': function(request, response) {
    $.ajax({
      {% if(VERSION >='4.0.2.0') %}
      url: 'index.php?route=catalog/product.autocomplete&user_token={{ user_token }}&filter_name=' +  encodeURIComponent(request),
      {% else %}
      url: 'index.php?route=catalog/product|autocomplete&user_token={{ user_token }}&filter_name=' +  encodeURIComponent(request),
      {% endif %}
      dataType:'json',
      success: function(json) {
         json.unshift({
          product_id: 0,
          name:'{{ text_none }}'
        });
        response($.map(json,function(item) {
          return {
            label: item['name'],
            value: item['product_id']
          }
        }));
      }
    });
  },
  'select': function(item) {
    $('input[name=\'form_product\']').val(item['label']);
    $('input[name=\'product_id\']').val(item['value']);
  }
});
</script>
{% if(VERSION>='4.0.2.0') %}
<script type="text/javascript"><!--

  $('textarea[data-oc-toggle=\'ckeditor\']').ckeditor();

// product
$('#input-product').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/product.autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['product_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-product').val('');
        $('#form-product-' + item['value']).remove();
        html = '<tr id="form-product-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_product[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-product tbody').append(html);
    }
});

$('#form-product').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});
// 

// Manufacturer
$('#input-form-product').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/product.autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                json.unshift({
                    product_id: 0,
                    name: '{{ text_none }}'
                });

                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['product_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-form-product').val(item['label']);
        $('#input-form-product-id').val(item['value']);
    }
});


// category
$('#input-category').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/category.autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['category_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-category').val('');

        $('#form-category-' + item['value']).remove();

        html = '<tr id="form-category-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_category[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-category tbody').append(html);
    }
});

$('#form-category').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});

// manufecture
$('#input-manufacturer').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/manufacturer.autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['manufacturer_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-manufacturer').val('');
        // console.log(item['value']);
        $('#form-manufacturer-' + item['value']).remove();

        html = '<tr id="form-manufacturer-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_manufacturer[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-manufacturer tbody').append(html);
    }
});

$('#form-manufacturer').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});
// 


       

//--></script>
 {% else %}
<script type="text/javascript"><!--

$('#input-product').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/product|autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['product_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-product').val('');
        $('#form-product-' + item['value']).remove();
        html = '<tr id="form-product-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_product[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-product tbody').append(html);
    }
});

$('#form-product').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});

// category
$('#input-category').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/category|autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['category_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-category').val('');

        $('#form-category-' + item['value']).remove();

        html = '<tr id="form-category-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_category[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-category tbody').append(html);
    }
});

$('#form-category').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});

// manufecture
$('#input-manufacturer').autocomplete({
    'source': function (request, response) {
        $.ajax({
            url: 'index.php?route=catalog/manufacturer|autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
            dataType: 'json',
            success: function (json) {
                response($.map(json, function (item) {
                    return {
                        label: item['name'],
                        value: item['manufacturer_id']
                    }
                }));
            }
        });
    },
    'select': function (item) {
        $('#input-manufacturer').val('');
        // console.log(item['value']);
        $('#form-manufacturer-' + item['value']).remove();

        html = '<tr id="form-manufacturer-' + item['value'] + '">';
        html += '  <td>' + item['label'] + '<input type="hidden" name="form_manufacturer[]" value="' + item['value'] + '"/></td>';
        html += '  <td class="text-end"><button type="button" class="btn btn-danger btn-sm"><i class="fas fa-minus-circle"></i></button></td>';
        html += '</tr>';

        $('#form-manufacturer tbody').append(html);
    }
});

$('#form-manufacturer').on('click', '.btn', function () {
    $(this).parent().parent().remove();
});
// 
       

//--></script>
{% endif%}

{{footer}}
</div>


<style>
.first li.active a,#form-form .first li.active a:hover,#form-form .first li.active a:focus{
  background-color: #f2f2f2;
}
#formfield {
    border-bottom: medium none;
    border-right: 4px solid #1e91ce;
    min-height: 906px;
}
 #formfield li.active > a,#form-form .nav-pills > li.active > a,#form-form .nav-pills > li.active > a:hover, #form-form .nav-pills > li.active > a:focus {
    border-color: transparent !important;
    background-color:transparent !important;
}
#formfield li a{
  color:#333;
}
 #formfield li.active {
    border-color: #1e91ce #fff #1e91ce #1e91ce;
    border-style: solid;
    border-width: 1px 4px;
    position: relative;
    right: -4px;
}
 .nav-tabs > li > a{
  font-size:13px;
}
a:focus {
    outline: medium none;
}
.fields .control-label{
  text-align:left;
}
#formfield-add a{
  color:#fff !important;
}
#formfield-add{
  margin-right:5px;
}
#formfield-add .btn-primary:hover,#formfield-add .btn-primary:focus,#formfield-add .btn-primary.focus,#formfield-add .btn-primary:active,#formfield-add .btn-primary.active,#formfield-add .open > .dropdown-toggle.btn-primary{
  color:#000 !important;
}
#formfield #formfield-add a{
  background-color:#1e91cf !important;
}
#formfield #formfield-add {
    border: medium none !important;
    margin-top:15px;
}
#formfield li a.active {
    position: relative;
}
#form-form #formfield li a {
    color: #333;
}
.nav-pills > li.active > a, .nav-pills > li.active > a:hover, .nav-pills > li.active > a:focus {
    color: #fff;
    background-color: #1e91cf;
}
.nav-pills > li > a {
    border-radius: 3px;
}
.nav > li > a {
    position: relative;
    display: block;
    padding: 10px 15px;
}
</style>
<script src="{{HTTP_CATALOG}}extension/tmdadvanceformbuilder/admin/view/javascript/colorbox/jquery.minicolors.js"></script>
<link rel="stylesheet" href="{{HTTP_CATALOG}}extension/tmdadvanceformbuilder/admin/view/stylesheet/jquery.minicolors.css">
<script>
$(document).ready( function() {
  $('.color').each( function() {
      $(this).minicolors({
      control: $(this).attr('data-control') || 'hue',
      defaultValue: $(this).attr('data-defaultValue') || '',
      inline: $(this).attr('data-inline') === 'true',
      letterCase: $(this).attr('data-letterCase') || 'lowercase',
      opacity: $(this).attr('data-opacity'),
      position: $(this).attr('data-position') || 'bottom left',
      change: function(hex, opacity) {
        if( !hex ) return;
        if( opacity ) hex += ', ' + opacity;
        try {
          console.log(hex);
        } catch(e) {}
      },
      theme: 'bootstrap'
    });

    });

});

</script>
<style>
#form-formbuilder .nav-tabs{
	gap:2px;
}
#form-formbuilder .nav-tabs .nav-link{
	background: #f5f5f5;
	border-color: #f5f5f5;
	color: #333;
	border-radius: 0;
}
#form-formbuilder .nav-tabs .nav-link i{
	margin-right:5px;
}
#form-formbuilder .nav-tabs .nav-link.active i,#formfield li a.active i{
	color: #fff !important;
}
#form-formbuilder .nav-tabs .nav-link.active,#formfield li a.active{
	background: #1C95DE;
	border-color: #1C95DE;
	color: #fff !important;
}
</style>